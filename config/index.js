import {systemInstruction,modelType} from "./geminiConfig.js"
import {uri,collectionName,dbName} from "./mongoConfig.js"

export{
    systemInstruction,
    modelType,
    uri,
    collectionName,
    dbName
}