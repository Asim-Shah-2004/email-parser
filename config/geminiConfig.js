const modelType = "gemini-1.5-flash"
const systemInstruction = `Given an email, read it and provide the email signature in 
    JSON format only provide the datin json dont write anything else make it json parsable
    dont write json at the top
`

export {
    modelType,
    systemInstruction
}